package com.example.collectalogger2.data.datasource

/**
 * This Data Source is for local games that are added manually and do not have an IGDB ID.
 * This is generally unlikely but may come up.
 */
class LocalDataSource {
    // the games should not be in a list, it should be persisted with Room.
    // TODO Implement this
}